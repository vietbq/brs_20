<%= form_for review.comments.build, remote: true, method: :post do |f| %>
  <%= f.hidden_field :review_id, value: review.id %>
  <%= f.label :comment %>
  <%= f.text_area :content, class: "form-control" %><br>

  <%= f.submit t("submit"), class: "btn btn-primary width_style" %>
<% end %>
